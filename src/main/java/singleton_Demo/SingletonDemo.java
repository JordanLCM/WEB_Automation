package singleton_Demo;

import singleton_Demo.Singleton;

public class SingletonDemo {

	public static void main(String[] args) {
	      Singleton tmp = Singleton.getInstance( );
	      tmp.demoMethod( );
	   }
}
